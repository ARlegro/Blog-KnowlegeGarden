---
{"dg-publish":true,"permalink":"/DevStudy/Backend/Java/Effective Java/Class & Interface/가능하면 클래스와 필드는 private으로 설계 해라/","noteIcon":"","created":"2025-12-03T14:52:49.155+09:00","updated":"2025-12-13T10:41:26.213+09:00"}
---



### 0.1.  잘 설계된 컴포넌트란❓

> 핵심 : 외부 컴포넌트로부터 얼마나 잘 숨겼느냐  이다. 

- 모든 내부 구현을 완벽히 숨김으로써 ➡ 구현과 API를 깔끔히 분리
- 오직!! API를 통해서만 다른 컴포넌트와 소통 
- 각 컴포넌트는 서로의 내부 동작 방식에 전혀 개의치 않는다.

> 이러한 개념이 캡슐화/정보 은닉 이다. 

>[!EXAMPLE] 정보 은닉의 장점
>1. **개발 속도를 높인다** : 여러 컴포넌트를 병렬로 개발할 수 있으므로
>2. **시스템 관리 비용을 낮춘다** : 디버깅에 용이하고 교체 부담도 적기 때문 
>3. **성능 최적화 가능성을 높여준다** : 다른 컴포넌트에 영향 없이 해당 컴포넌트에만 집중해서 최적화 가능 
>4. **재사용성을 높여준다.**
>5. **난이도 감소** : 개별 컴포넌트 검증에 용이하므로 (특히 규모가 커질수록 용이)


### 0.2.  핵심 : 모든 클래스, 멤버의 접근성을 최대한 좁히는 것 

- 패키지 외부에서 쓸 이유가 없다❓ >> 'package-private'
- 패키지 외부에서 쓸 이유가 없지만 상속은 염두 ❓ >> protected 

>[!tip] 핵심 : 접근 가능성을 최대한 줄이는 것 ex.  public 줄이기
>- 외부에 공개한 순간 API가 되므로 변경 시 호환을 위해 영원히 관리해줘야 한다.
>- 만약 어느 정도 풀어줘야 한다면 package-private까지는 풀어주자 
>- 💢권한을 자주 풀어줘야 한다면 더 분해하는게 어떤지 고민해라


> [!WARNING] 주의 : 상위 클래스 메서드 Override 시 그 접근 수준을 상위 클래스보다 좁게 설정할 수 없다.
> 그렇지 않으면❓
>- '상위 클래스 인스턴스는 하위 클래스 인스턴스로 대체할 수 있어야 한다'는 **리스코프 치환 원칙 위배** 💢



### 0.3.  Public 클래스의 인스턴스 필드는 되도록 public이 아니어야 한다.

> [!WARNING] 안전하지 않은 필드
> 1. 가변 객체를 참조하는 필드 (final이여도 참조 객체는 수정가능하므로 문제)
> 2. final이 아닌 필드 

위의 필드들의 문제
1. 불변식을 보장할 수 없다. 
2. Thread-Safe하지 않다.

public을 써도 되는 예외
- static final 같이 필요한 구성요소로써의 상수일 때
#### 0.3.1.  헷갈릴만한 코드 - 컬렉션을 final로 ❓
>  배열은 모두 변경 가능하니 static final로는 의미가 없다. 접근 제어자로 막아야 한다.

```java
❌허점이 있는 코드 
public static final Thing[] VALUES = { ... }

✅해결 방법 1. private으로 배열을 만든 뒤 접근은 불변 객체로 하도록 하기 
private static final Thing[] PRIVATE_VALUES = { ... } ;
publci static final List<String> VALUES = 
				Collections.unmodifiableList(Arrays.asList(PRIVATE))

✅해결 방법 2. 방어적 복사 - Client가 복사본으로 외부에서 수정하는 걸 원할 경우
private static final Thing[] PRIVATE_VALUES = { ... } ;
publci static final Thing[] VALUES = 
				return PRIVATE_VALUES.clone();
```
- 단순히 배열을 `static final`로 한다해도 values 내부를 수정할 수 있다.

