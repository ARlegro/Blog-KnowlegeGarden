---
{"dg-publish":true,"permalink":"/DevStudy/Backend/Java/객체 지향 생활 체조 원칙/","noteIcon":"","created":"2025-12-03T14:52:49.323+09:00","updated":"2025-12-13T10:40:38.431+09:00"}
---


> 객체 지향 생활 체조 원칙 


>[!EXAMPLE] 규칙 
>- 1 : 한 메서드에 오직 한 단계의 들여쓰기만 한다.
>- 2 : else를 쓰지 않는다.
>- 3: 모든 원시값과 문자열을 포장한다.
>- 4: 한 줄에 점을 하나만 찍는다.
>- 5: 줄여 쓰지 않는다(축약 금지).
>- 6: 모든 엔티티를 작게 유지한다.
>- 7: 3개 이상의 인스턴스 변수를 가진 클래스를 쓰지 않는다.
>- 8: 일급 컬렉션을 쓴다.
>- 9: getter/setter/property를 쓰지 않는다.


### 0.1.  한 메서드에 오직 한 단계의 들여쓰기만 한다
> 중첩된 if, for, while 등을 여러 번 쓰지 말자.

#### 0.1.1.  개념 
들여쓰기가 여러 번 등장하는 메서드는 하나의 책임(SRP)을 벗어난 경우가 많다.
이는 아래와 같은 **단점**을 가진다:
- 가독성을 해친다
- 테스트하기 어렵다
- 디버깅하기 어렵다 
- 재사용성이 낮다 

들여쓰기를 최소화하면 자연스럽게 메서드를 분리하게 되고 결과적으로 더 작고 단일한 책임을 가진 메서드들이 만들어지게 된다.
>메서드는 역할이 적을 수록 좋다.


#### 0.1.2.  나쁜 예 💢

```java
public void badProcess(List<Item> items) {
		for (Item item : items){
			if (item.isValid()) {
				increasePrice();
			} else {
				decreasePrice();
			}
		}
}
```

#### 0.1.3.  좋은 예 ✅
```java
public void goodProcess(List<Item> items) {
		for (Item item : items){
			processPricing(item);
		}
}

public void processPricing(Item item){
		if (!item.isValid(){
				decreasePrice(item)
				return;
		}

		increasePrice(item);
}
```




### 0.2.  else 를 사용하지 않는다.
> if-else 구조는 코드를 복잡하게 만든다.


이건 너무나도 이전부터 재밌게 보고 지켜왔기에 PASS


### 0.3.  모든 원시값과 문자열을 포장한다.



### 0.4.  한 줄에 점을 하나만찍는다.

여기서 말하는 점(.)은 메서드 호출 or 속성에 접근하는 점이다.
점이 연속된다면 **객체 내부 구조에 지나치게 의존한다는 의미라 캡슐화가 깨져있을 가능성이 높다.**
```java
💢나쁜 예 
user.getProfile().getAddress()

✅좋은 예 
Address address = user.getAddress();
```


### 0.5.  줄여 쓰지 않는다(축약 금지)

약어는 타인이 이해하기 어렵고, 시간이 지나서 본인도 못 알아 볼 확률이 높다.
가독성을 위해 변수, 메서드명은 최대한 풀어쓴다.

```java
❌ 나쁜 예 
calculStocks()

✅ 좋은 예 
calculateStocks()
```

### 0.6.  3개 이상의 인스턴스 변수를 가진 클래스를 쓰지 않는다

지나치게 많은 변수를 갖고 있는 클래스는 너무 많은 상태를 관리한다는 뜻이다.

```JAVA 
❌ 나쁜 예 
public class User {

		private Long id;
		private String name;
		private String city;
		private String street;	
		private String zipcode;
}

✅ 좋은 예 
public class User {

		@Id
		private Long id;
		private String name;

		@Embedded
		private Address address;
}

public class Address {
		
		private String city;
		private String street;	
		private String zipcode;
}
```


> [!WARNING] 만약 못 지키겠다면?
> 아래의 사항들을 고려해라
> 1. 필드가 많아져도 SRP는 잘 지켜지는가 ❓
> 	- 각 필드가 서로 밀접하게 연관되어 있다면 응집도면에서 좋은 거라 개수가 많아지더라도 의미가 있다.
> 2. 가독성 유지보수성





